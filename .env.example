# Environment Configuration Template

# Copy this file to .env.local and update with your actual values

# ============================================
# MongoDB Configuration
# ============================================

# Option 1: Local MongoDB (Default)
MONGODB_URI=mongodb://localhost:27017/wedding-invite

# Option 2: MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/wedding-invite?retryWrites=true&w=majority

# To use MongoDB Atlas:
# 1. Go to https://www.mongodb.com/atlas
# 2. Create a free account and cluster
# 3. Create a database user
# 4. Get your connection string
# 5. Replace <username>, <password>, and <cluster> with your values

# ============================================
# Session Configuration (Optional)
# ============================================

# Change this to a secure random string in production
SESSION_SECRET=your-secret-key-change-this-in-production

# ============================================
# Production Deployment
# ============================================

# When deploying to Vercel, Netlify, etc.:
# 1. Add MONGODB_URI as an environment variable in your hosting platform
# 2. Use MongoDB Atlas (cloud) instead of local MongoDB
# 3. Set NODE_ENV to "production" (usually automatic)
